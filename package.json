{
  "name": "crystal-knights",
  "version": "1.0.0",
  "private": true,
  "description": "GameBoy風レトロJRPG クリスタルナイツ (iOSラッパー)",
  "scripts": {
    "ios:setup": "npx cap add ios && npx cap sync ios",
    "ios:sync": "npx cap sync ios",
    "ios:open": "npx cap open ios"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0",
    "@capacitor/ios": "^6.0.0"
  }
}
